#include<stdio.h>
#include<math.h>
int main(){
    float n;
    printf("Enter a number :- ");
    scanf("%f",&n);
    float y = sqrt(n); //sqrt is inbuilt function in c
    printf("the square root is :- %f",y);
    return 0;
}